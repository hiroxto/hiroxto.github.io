language : node_js
node_js:
    - "9"
    - "10"

before_script :
    - npm install

script :
    - exit 0

cache :
    directories :
        - node_modules

notifications :
    on_success : never
    on_failure : never
